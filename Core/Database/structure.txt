CREATE TABLE user_project ( user_id INT , project_id INT );
CREATE TABLE user ( id INT NOT NULL AUTO_INCREMENT PRIMARY KEY, nom VARCHAR(255) NOT NULL, email VARCHAR(255) NOT NULL, password VARCHAR(255) NOT NULL, roles LONGTEXT COMMENT "json_array" NOT NULL, validationDate DATETIME, extension_id INT , description TEXT );
CREATE TABLE extension ( id INT NOT NULL AUTO_INCREMENT PRIMARY KEY, age INT NOT NULL, telephone VARCHAR(255) NOT NULL, adresse VARCHAR(255) NOT NULL );
CREATE TABLE project ( id INT NOT NULL AUTO_INCREMENT PRIMARY KEY, nom VARCHAR(255) NOT NULL, date DATE NOT NULL );
ALTER TABLE user ADD CONSTRAINT FK_extension FOREIGN KEY (extension_id) REFERENCES extension(id);
ALTER TABLE user_project ADD CONSTRAINT FK_project FOREIGN KEY (project_id) REFERENCES project(id);
ALTER TABLE user_project ADD CONSTRAINT FK_user FOREIGN KEY (user_id) REFERENCES user(id);